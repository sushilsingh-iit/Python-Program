# Nesting simply means putting one construct inside of another. Think of it like a set of Russian Matryoshka dolls—you open one doll, and there is another smaller one inside it.
# In Python, you can nest almost anything: if statements, loops, lists, and even functions.

age = 82
if (age >=18 ):
    if (age >=80):
        print ("can't drive ")

    else:
        print ("can drive")

else:
    print ("can not drive ")